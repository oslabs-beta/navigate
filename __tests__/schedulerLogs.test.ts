import * as thing from '../server/schedulerLogs';

describe("test schedulerLogs command", () => {
  thing.getDeploymentNames();
  // it('should create a file called SomeFile.txt', () => {

  // });
});